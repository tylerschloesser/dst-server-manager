// @dst/api: Lambda handlers (auth, worlds, start/stop) and the reaper; local dev server
// (docs/control-plane.md §1). Filled in by later tasks; this scaffold only proves the package
// compiles and is importable through its `exports` map.
export const API_PACKAGE_NAME = '@dst/api';
