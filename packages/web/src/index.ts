// @dst/web: Vite + React 19 + Mantine 8 SPA (docs/web.md §1 / §2). Filled in by later tasks
// (src/main.tsx, src/App.tsx, ...); this scaffold only proves the package compiles.
export const WEB_PACKAGE_NAME = '@dst/web';
