// @dst/supervisor: on-instance entrypoint — env -> adapters -> loop (docs/game-server.md §1).
// Filled in by later tasks; this scaffold only proves the package compiles and bundles.
export const SUPERVISOR_PACKAGE_NAME = '@dst/supervisor';
