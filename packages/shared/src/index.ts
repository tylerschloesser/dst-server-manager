// @dst/shared: types + constants shared by every package (docs/control-plane.md §1).
// Filled in by later tasks; this scaffold only proves the package compiles and is importable
// through its `exports` map.
export const SHARED_PACKAGE_NAME = '@dst/shared';
