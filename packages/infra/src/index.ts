// @dst/infra: CDK app (DstCi, DstGame, DstWeb) (docs/infra.md §1). `bin/app.ts` and `lib/*.ts`
// land in a later task; this scaffold only proves the package compiles.
export const INFRA_PACKAGE_NAME = '@dst/infra';
